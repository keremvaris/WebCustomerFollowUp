@model WebCustomerFollowUp.Models.Job

@{
    ViewBag.Title = "Ayrıntılar";
}

<h2>Ayrıntılar</h2>

<fieldset>
    <legend>Job</legend>

    <div class="display-label">Müşteri</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.Customer.Name)
    </div>

    <div class="display-label">Website</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.Website)
    </div>

    <div class="display-label">İş</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.Job1)
    </div>

    <div class="display-label">Notlar</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.Notes)
    </div>
</fieldset>
<p>
    @Html.ActionLink("Düzenle", "Edit", new { id=Model.ID }) |
    @Html.ActionLink("Listeye Geri Dön", "Index")
</p>
