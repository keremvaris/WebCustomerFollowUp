@model WebCustomerFollowUp.Models.Renk

@{
    ViewBag.Title = "Ayrıntılar";
}

<h2>Ayrıntılar</h2>

<fieldset>
    <legend>Renk</legend>

    <div class="display-label">Renk</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.renk1)
    </div>
</fieldset>
<p>
    @Html.ActionLink("Düzenle", "Edit", new { id=Model.id }) |
    @Html.ActionLink("Listeye Geri Dön", "Index")
</p>
